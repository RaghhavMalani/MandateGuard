.env
.env.*
!.env.example
__pycache__/
*.py[cod]
.pytest_cache/
.pytest_tmp/
.venv/
*.egg-info/
build/
dist/
artifacts/engineering/semantic_mvp/*.jsonl
artifacts/engineering/semantic_mvp/cache/

# Raw dataset archives. Large, redistributable only under their own licence, and
# reproducible with `python scripts/import_discovery_catalog.py`. What the
# repository carries instead is the normalized catalog plus its provenance
# manifest.
data/import/

# Local dev convenience (browser preview launcher).
.claude/
